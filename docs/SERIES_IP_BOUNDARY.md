# Series IP Exclusion Boundary

Commercial systems must remain independent from the private CrossingKey fictional series.

The company/technical brand may use the CrossingKey name. That does not authorize inclusion of fictional-series material.

Before SECURITY_REVIEWED:
1. Run the series-IP scanner against candidate adapters, docs, examples, fixtures, prompts, outputs, and marketing metadata.
2. Investigate every match.
3. Do not promote on uncertainty.

Before PRODUCTION_ENABLED:
- attach an operator attestation that the candidate is series-neutral.

The scanner is configurable. Keep private character/location/lore identifiers in the local pattern file rather than embedding them into public MCP descriptions.
